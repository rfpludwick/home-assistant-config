alias: "Notification: Crime Change"
initial_state: true
trigger:
  - platform: state
    entity_id: sensor.crime
action:
  - service: notify.slack_crit
    data:
      message: "Crime changed to '{{ states.sensor.crime.state }}'"
