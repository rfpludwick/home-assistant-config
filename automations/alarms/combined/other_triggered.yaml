alias: "Alarms: Combined Alarm: Other Triggered"
trigger:
  - platform: state
    entity_id: alarm_control_panel.vierarlo
    to: triggered
  - platform: state
    entity_id: alarm_control_panel.scout_alarm
    to: triggered
condition:
  - condition: state
    entity_id: input_boolean.automation_combined_alarm
    state: "on"
  - condition: template
    value_template: "{{ (states.alarm_control_panel.combined_alarm.state != 'triggered') }}"
action:
  - service: alarm_control_panel.alarm_trigger
    entity_id: alarm_control_panel.combined_alarm
    data:
      code: !secret alarm_code
